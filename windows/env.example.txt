# AI秘書 設定ファイル（.env）
# 1_setup.bat が対話で埋めます。手で直したいときはメモ帳で開いて編集し、
# 3_start.bat を実行し直してください（= 停止して起動し直す）。
# ※ このファイルは他人に絶対に見せないこと。鍵がそのまま入っています。

# --- LINE Messaging API（LINE Developers で取得）---
LINE_CHANNEL_ACCESS_TOKEN=
LINE_CHANNEL_SECRET=
# 朝の通知の宛先。空でも登録・変更・削除は動く。4_set_user_id.bat が自動で埋める
LINE_USER_ID=

# --- Gemini API（Google AI Studio で取得）---
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# --- Google カレンダー ---
GOOGLE_CALENDAR_ID=primary
GOOGLE_CREDENTIALS_PATH=credentials.json
GOOGLE_TOKEN_PATH=token.json

# --- ローカルサーバー ---
# ngrok が同じPCから繋ぐので 127.0.0.1 でよい（Windowsファイアウォールの警告も出ない）
WEBHOOK_HOST=127.0.0.1
WEBHOOK_PORT=5555
VERIFY_SIGNATURE=true

# --- ngrok（固定ドメイン。例: happy-cat-1234.ngrok-free.dev）---
NGROK_DOMAIN=
