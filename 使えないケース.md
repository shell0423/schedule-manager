# 使えないケース・制約の一覧

「LINE に送ったのに入っていない」「通知が来ない」と思ったときに最初に見る文書。
2026-08-20 時点のコードとログを確認して書いた。運用を変えたら追記すること。

配布版（友人に渡す ZIP）にも同じ趣旨の文書を入れてある
（Mac 版＝`mac/使えないケース.md`、Windows 版＝`windows/0_START_HERE.html` 内の節）。

---

## 先に：外出中は問題にならない

**あなたが Mac から物理的に離れていることは、一切関係ない。**
LINE → ngrok → 自宅の Mac という経路なので、Mac が起きてネットに繋がってさえいれば
海外からでも同じように動く。手元にパソコンが要る場面は無い。

問題になるのは「Mac から遠いこと」ではなく、**「Mac が動いていないこと」**だけ。

---

## A. メッセージが届かない（送っても消える）

ここが本質的な弱点。**LINE は届かなかった Webhook を再送しない。**
この状態で送ったメッセージは、後から Mac を起こしても**永久に失われる**（キューに溜まらない）。

| ケース | 補足 |
|---|---|
| **Mac がシャットダウン / スリープ** | 最も多い。MacBook はふたを閉じるとスリープする |
| **再起動後、ログインしていない** | 見落としやすい。常駐は LaunchAgent＝**ログイン時**に起動するもので、電源投入時ではない。ログイン画面で放置＝動いていない。FileVault 有効だと特に該当する |
| 自宅のネット回線が落ちている | ルーター・ISP 障害 |
| ngrok の同時接続超過 | 無料枠は**同時1セッション**。他所で同じアカウントの ngrok を起動すると奪われる（`ERR_NGROK_108`） |
| ngrok 側の障害 | 稀。`logs/ngrok.log` に出る |

### 失敗の見分け方

**`✅ 登録しました` の返信が来なかったら、入っていない。**
LINE 側は送信済みに見えるのでエラーは出ない。返信の有無だけが確実な判断材料。

---

## B. 受け付けるが、カレンダーに入らない

| ケース | 症状 | 復旧 |
|---|---|---|
| **Google 認証切れ** | 「⚠️ カレンダーに接続できません」が返る／朝の通知の代わりに同じ警告が1日1回届く | `.venv/bin/python -m src.calendar_client` で再認証 |
| **Gemini の支出上限超過** | 「⚠️ Gemini の月間支出上限に達しています」 | https://ai.studio/spend で上限を上げる（毎月1日にリセット） |
| Gemini のレート制限 | 「⚠️ Gemini の利用上限に達しています」 | 時間をおく |
| Gemini のキー無効 | 「解析に失敗しました」 | キーを入れ直す |
| LINE のアクセストークン再発行 | 返信自体が来なくなる（401） | `.env` を更新して再起動 |

**Google の OAuth 同意画面が「テスト」のままだと、リフレッシュトークンが7日で失効し続ける。**
「本番環境」にしてあれば通常は切れない。繰り返し認証切れになるならここを疑う。

**Gemini の支出上限は「キー」ではなく「GCP プロジェクト」単位。**
`GEMINI_API_KEY` は `~/Claude/.shared-secrets/gemini.env` 経由で
**PTS上位下位調査 / モメンタム戦略ダッシュボード / KPI抽出関連**と共有しているため、
**他のプロジェクトが枠を使い切るとこのアプリも巻き添えで止まる**。
2026-08-21 に実際に発生した（このアプリ自身の消費は5/23以降で117回・3円程度）。
恒久対策は、このアプリだけ別プロジェクトの鍵に分けること。

---

## C. 通知が来ない・時刻がずれる

- **その日の予定が0件なら送らない**（仕様）
- **1日1回まで**。`schedule.db` の `notification_log` で抑止している
- **スリープしていると復帰時にまとめて実行される。** 夜に「今日の予定」が届くことがある。
  実際 2026-08-08 は 09:04、08-13 は 08:04 に走っていた（`logs/notifier.err.log`）
- LINE 無料プランの push は月200通まで。**自分への返信は数えられず、数えるのは朝の通知だけ**
  なので月30通程度。通常は当たらない

---

## D. セキュリティ上の前提

**LINE 公式アカウントの ID は他人に教えないこと。** ID を知った人は友だち追加できる。
`LINE_USER_ID` を設定してあれば本人以外のメッセージは無視されるが、**未設定の間は
誰でもカレンダーを操作できる**（初回セットアップを回すための仕様）。
`status` が未設定を警告するので、そこが「失敗」なら最優先で直す。

---

## E. 機能としてできないこと

コードを読んで確認した制約（`src/calendar_client.py` / `src/main.py`）。

- **過去の予定は変更・削除できない。** `search_event` の起点が「現在時刻」なので
  「先週の会議を消して」は見つからない（日付を明示すればその前後1日は探す）
- **60日より先の予定は、日付を言わないと掴めない。** 日付なしの検索範囲は今から60日。
  「名古屋ドームを19時に変更」だけでは範囲外。「6/14の名古屋ドームを…」なら通る
- **同名の予定が複数あると最初の1件**を選ぶ。狙ったものと違うことがある
- **スタンプ・画像・音声・位置情報は完全に無反応**（テキストのみ処理・返信も来ない）
- **通知先は1人だけ／カレンダーは1つだけ**（`LINE_USER_ID` と `GOOGLE_CALENDAR_ID` が各1）
- 参加者の招待、リマインダー時刻の指定、繰り返し予定の一部だけ変更 は未対応
- 曖昧な日本語は誤読される（「夕方」「来週あたり」など）。日付と時刻を具体的に書くと安定する

---

## 対策の優先順位

1. **電源接続時にスリープさせない**（システム設定 → ロック画面）。A が本質的な弱点なので、
   これが最も効く。ノートを持ち歩く運用なら、そもそも据え置き機で動かす方が向いている
2. **OAuth 同意画面を「本番環境」にしておく**（B の再発防止）
3. 取りこぼしを根本的に無くしたいなら **VPS へ移す**。Mac のスリープも ngrok の
   同時1セッション制約も消える。今の構成のまま載せ替え可能（Nginx + gunicorn 構成）
