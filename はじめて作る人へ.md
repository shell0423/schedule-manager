# LINE「AI秘書」を一から作る ― 完全初心者ガイド

このアプリは、**LINEに「明日10時に歯医者」と送るだけでGoogleカレンダーに自動登録され、毎朝8時に今日の予定がLINEで通知される**、自分専用のAI秘書です。

このガイドは **プログラミング経験ゼロ・AI使ったことなし** の人が、**Claude Codeに作ってもらう前提**で、自分のMacに同じものを作るための手順書です。

> 💡 **ポイント**: コードは1行も書きません。あなたがやるのは「鍵の取得」「コピペ」「動作確認」だけ。実装は全部Claude Codeに任せます。

> ⚠️ **このガイドの対象**: **Mac** ＋ **Claude Code を持っている人**が、同じものを一から作り直す場合の手順です。
> - **Windows の人／Claude Code を持っていない人** → 完成品の ZIP を渡す方式のガイド `windows/0_START_HERE.html` を使ってください。
> - **STEP 1（LINE）の手順は古くなっています**。現在は先に [LINE Official Account Manager](https://manager.line.biz/) で公式アカウントを作り、その設定画面から Messaging API を有効化する流れです。最新の手順は `windows/0_START_HERE.html` の STEP 1 が正しいので、そちらを見てください。

---

## 目次

1. [このアプリでできること](#1-このアプリでできること)
2. [所要時間・必要なもの・かかるお金](#2-所要時間必要なものかかるお金)
3. [全体像（仕組みの図）](#3-全体像仕組みの図)
4. [STEP 0：道具を揃える](#step-0道具を揃える)
5. [STEP 1：LINE公式アカウント「AI秘書」を作る](#step-1lineの公式アカウントai秘書を作る)
6. [STEP 2：Googleカレンダーを使えるようにする](#step-2googleカレンダーを使えるようにする)
7. [STEP 3：Gemini（AI）の鍵を取る](#step-3geminiaiの鍵を取る)
8. [STEP 4：ngrok（外からMacに繋ぐ仕組み）を準備する](#step-4ngrok外からmacに繋ぐ仕組みを準備する)
9. [STEP 5：Claude Codeにアプリを作ってもらう](#step-5claude-codeにアプリを作ってもらう)
10. [STEP 6：設定ファイル `.env` を埋める](#step-6設定ファイル-env-を埋める)
11. [STEP 7：常時起動の設定（LaunchAgent）](#step-7常時起動の設定launchagent)
12. [STEP 8：LINEに「外からの入口」を教える](#step-8lineに外からの入口を教える)
13. [STEP 9：動作テスト](#step-9動作テスト)
14. [よくあるエラーと対処](#よくあるエラーと対処)
15. [改造のヒント](#改造のヒント)

---

## 1. このアプリでできること

LINEで自由に文章を送ると、AIが内容を理解してGoogleカレンダーを操作します。

| あなたが送る | アプリの動き |
|---|---|
| `今日14時から会議` | カレンダーに登録 |
| `明日の10時に歯医者` | 登録 |
| `4/25 Aさんにメール` | 登録（時刻なし→9時） |
| `8/11-16 夏季休暇` | 終日予定（6日間）として登録 |
| `毎週月曜10時にチーム会議` | 繰り返し予定として登録 |
| `4/24の打ち合わせを16時に変更` | 既存予定を変更 |
| `金曜の会食キャンセル` | 削除 |
| `今週の予定は？` | 一覧をLINEで返信 |

加えて、**毎朝8時に当日の予定**、**月曜の朝に今週分**を自動でLINE通知してくれます。

---

## 2. 所要時間・必要なもの・かかるお金

### 所要時間
半日〜1日。ほとんどは「ブラウザでアカウントを作る」「届いたメールを認証する」時間です。

### 必要なもの
- **Mac** （Windowsは別途調整が必要なのでこのガイドの対象外）
- **インターネット接続**
- **クレジットカード**（Google Cloudの登録に必要、ただし無料枠で収まる）
- **LINEアカウント**（普段使っているもの）
- **Googleアカウント**（普段使っているもの）
- **Claude Pro / Max プラン** または **Anthropic APIキー**（Claude Codeを使うため）

### 月額コスト目安
| サービス | 料金 |
|---|---|
| LINE Messaging API | 無料（月200通まで。1000通までなら月額1,000円程度のプランあり） |
| Google Calendar API | 無料 |
| Gemini API（gemini-2.5-flash） | 1回の解析≈0.03円、個人利用なら月数十円 |
| ngrok（無料の固定URL） | 無料 |
| Claude Code | 別契約（Proなら月$20、Maxなら月$100〜） |

**Claude Code料金を除けば月100円もかかりません。**

---

## 3. 全体像（仕組みの図）

```
あなた
  │ LINEで「明日10時に歯医者」と送信
  ▼
LINE「AI秘書」公式アカウント
  │ メッセージを「Webhook URL」というアドレスに転送
  ▼
ngrok（インターネット → あなたのMac の入口）
  │
  ▼
Macで動いているPythonプログラム（Flask）
  ├─ Geminiに「これ何の予定？」と聞く
  ├─ Googleカレンダーに登録する
  └─ LINEに「✅ 登録しました」と返信する

毎朝8:00 → Macが自動でカレンダーを見て、当日の予定をLINEに送る
```

カタカナが多くて怖いですが、ひとつずつ「アカウントを作って鍵をもらう」だけなので大丈夫です。

---

## STEP 0：道具を揃える

### 0-1. ターミナルを開けるようにする

「ターミナル」というのは、Macに文字でコマンドを打つアプリです。
1. `command + space` でSpotlightを開く
2. `ターミナル` と入力してEnter
3. 黒い画面（または白い画面）が出てくればOK

このガイドで「**ターミナルで○○と打つ**」と書いてあったら、ここに貼り付けてEnter、という意味です。

### 0-2. Homebrewを入れる

Homebrew = Macに便利なツールを入れるための「アプリストア」みたいなもの。

ターミナルに以下を貼り付けてEnter:
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

途中でパスワードを聞かれたらMacのログインパスワードを入れます（画面には表示されないけど打てています）。

### 0-3. Python と Claude Code を入れる

```bash
brew install python@3.11 node
npm install -g @anthropic-ai/claude-code
```

入れ終わったら確認:
```bash
python3 --version       # Python 3.11.x と出ればOK
claude --version        # バージョン番号が出ればOK
```

### 0-4. プロジェクトのフォルダを作る

```bash
mkdir -p ~/Claude/スケジュール管理
cd ~/Claude/スケジュール管理
```

以降、この `~/Claude/スケジュール管理` フォルダで作業します。

---

## STEP 1：LINEの公式アカウント「AI秘書」を作る

### 1-1. LINE Developersに登録

1. https://developers.line.biz/ja/ にアクセス
2. 右上「**コンソールにログイン**」→ 普段のLINEアカウントでログイン
3. 開発者名・メールアドレスを聞かれたら適当に入力（自分の名前でOK）

### 1-2. プロバイダーを作る

「プロバイダー」 = アカウントの持ち主の名前。自分の名前で1つ作ります。
1. 「**Create a new provider**」をクリック
2. プロバイダー名: `自分の名前` など（後から変更可）
3. 「Create」

### 1-3. Messaging APIチャネルを作る

「チャネル」 = LINEの公式アカウント1つに対応するもの。
1. プロバイダーを開いた画面で「**Create a Messaging API channel**」
2. 設定:
   - **Channel name**: `AI秘書`
   - **Channel description**: `自分用のAI秘書`
   - **Category** / **Subcategory**: 個人なら適当に
   - 利用規約に同意してCreate

### 1-4. 鍵を2つコピーしておく

作成したチャネルを開いて、後で使う鍵をメモ帳に保存しておきます:

| タブ | 項目 | メモ名 |
|---|---|---|
| Basic settings | **Channel secret** | `LINE_CHANNEL_SECRET` |
| Messaging API | **Channel access token** | `LINE_CHANNEL_ACCESS_TOKEN` |

- Channel access tokenは「Issue」ボタンで発行
- 両方とも長い文字列。**他人に絶対見せない**。

### 1-5. LINEで友だち追加して User ID を取る

Messaging APIタブにQRコードがあるので、自分のスマホのLINEで読んで友だち追加します。

「自分のUser ID」を取るには少し工夫が要ります。後でClaude Codeに「LINEのWebhookで `userId` をログに出して」と頼めば取れるので、いったん飛ばしてOK。

### 1-6. 自動返信をオフにする

LINEの公式アカウントは、デフォルトで「ありがとうございます」みたいな自動応答が入っています。これを切ります。
1. Messaging APIタブの一番下「**LINE Official Account features**」の「**Edit**」
2. 「**応答メッセージ**」→ オフ
3. 「**あいさつメッセージ**」→ お好み
4. 「**Webhook**」→ **オン**（重要）

---

## STEP 2：Googleカレンダーを使えるようにする

### 2-1. Google Cloud プロジェクトを作る

1. https://console.cloud.google.com/ にアクセス
2. 上のバー → 「**プロジェクトを選択**」→ 「**新しいプロジェクト**」
3. プロジェクト名: `AI秘書` など。Createをクリック

### 2-2. Calendar APIを有効化

1. 左上ハンバーガーメニュー → 「APIとサービス」 → 「**ライブラリ**」
2. 検索欄に `Google Calendar API`
3. 出てきたものを開いて「**有効にする**」

### 2-3. OAuth同意画面を「本番」に設定する

> ⚠️ **これを必ずやってください。** テストモードだとアプリの認証情報が**7日で勝手に失効する**ので、毎週エラーになります。

1. 左メニュー「**Google Auth Platform**」 → 「**対象**」
2. **公開ステータス: テスト** と表示されていたら「**アプリを公開**」ボタンを押す → 確認
3. **公開ステータス: 本番環境** になればOK

個人スコープ（カレンダー）は審査なしで本番にできます。

### 2-4. OAuth クライアントIDを作る

「OAuthクライアントID」 = アプリがあなたのGoogleカレンダーにアクセスするための鍵。

1. 左メニュー「**Google Auth Platform**」→「**クライアント**」
2. 「**+ クライアントを作成**」
3. アプリケーションの種類: **デスクトップアプリ**
4. 名前: `AI秘書 desktop` など
5. 作成
6. ダイアログで「**JSONをダウンロード**」をクリック
7. ダウンロードされたファイルを `~/Claude/スケジュール管理/credentials.json` に**ファイル名を変えて**保存

### 2-5. 自分をテストユーザーに追加（本番モードでも一応）

「対象」画面の「テストユーザー」セクションに自分のGmailアドレスを追加しておきます（万一の保険）。

---

## STEP 3：Gemini（AI）の鍵を取る

1. https://ai.studio/ にアクセス → Googleアカウントでログイン
2. 左メニュー「**Get API key**」
3. 「**Create API key**」
4. 適当なプロジェクト（さっき作った `AI秘書` でOK）を選んで作成
5. 出てきた鍵をメモ。これが `GEMINI_API_KEY`

### 月の上限を設定（重要・暴走防止）

1. https://ai.studio/spend を開く
2. プロジェクトを選択
3. 「**費用の上限を編集**」→ 月額¥1000など、自分が許せる額に設定

これでAIが勝手に高額請求を発生させる事故を防げます。

---

## STEP 4：ngrok（外からMacに繋ぐ仕組み）を準備する

「ngrok」 = あなたのMacの中で動いているプログラムを、インターネットから触れるようにする道具。**家のWi-Fiでも使えます**。

### 4-1. ngrokを入れる

```bash
brew install ngrok
```

### 4-2. ngrokのアカウント作成

1. https://ngrok.com/ にアクセス → サインアップ
2. ダッシュボードに表示される「**Your Authtoken**」をコピー
3. ターミナルで:
```bash
ngrok config add-authtoken ここに貼り付け
```

### 4-3. 固定URLを取得（無料）

無料アカウントでもURLを1つ固定でもらえます。

1. ngrokダッシュボード左メニュー「**Domains**」
2. 「**+ New Domain**」 → 自動で `xxxxx-xxxxx.ngrok-free.dev` のような名前がもらえる
3. メモしておく。これが**あなたのアプリの公開URL**

---

## STEP 5：Claude Codeにアプリを作ってもらう

ここからが本番です。**コードは書きません**。プロンプトを貼り付けてClaude Codeに作ってもらいます。

### 5-1. Claude Codeを起動

```bash
cd ~/Claude/スケジュール管理
claude
```

### 5-2. 以下のプロンプトを丸ごと貼り付ける

````
以下の仕様で、Mac上で動くLINE Bot「AI秘書」を作ってください。
コードは src/ 配下に、テストは tests/ 配下に置いてください。
作成後、起動方法と動作確認手順を教えてください。

## 概要
LINE公式アカウントに自由文を送ると、Geminiが解析してGoogleカレンダーに登録/変更/削除する。
毎朝8:00に当日の予定、月曜は今週分もLINE Pushで通知する。

## 技術スタック
- Python 3.11（Flask）
- LINE Messaging API（公式SDKは使わずrequestsで直叩き、署名検証は自分で実装）
- Google Calendar API（OAuth User Credentials, credentials.json + token.json）
- Gemini API（google-genai SDK、モデルは gemini-2.5-flash、response_mime_type=application/json）
- SQLite（通知済みフラグ・予定の冪等管理）
- ngrok（dev domain 固定）
- macOS LaunchAgent 3つで常駐 + 定時実行

## ファイル構成
- src/main.py: Flask Webhook (/webhook/line, /healthz)
- src/parser.py: Gemini自由文解析（action=create/update/delete/list/unknown を返すJSON）
- src/calendar_client.py: Google Calendar APIの薄いラッパー（create/update/delete/list/search）
- src/line_client.py: LINE署名検証、reply、push
- src/notifier.py: 朝の通知バッチ
- src/db.py: SQLite
- src/config.py: 環境変数読み込み

## Gemini出力JSONスキーマ
{
  "action": "create|update|delete|list|unknown",
  "all_day": true|false,
  "start_at": "YYYY-MM-DDTHH:MM:00 または all_day=true なら YYYY-MM-DD",
  "end_at": "同上（all_day時は含む最終日）",
  "title": string,
  "description": string,
  "recurrence": "RRULE本体 (FREQ=...形式) または null",
  "search_query": "update/deleteの探索キーワード",
  "search_date": "YYYY-MM-DD",
  "list_period": "today|tomorrow|this_week|next_week|date",
  "list_date": "YYYY-MM-DD",
  "message": "ユーザーへの確認メッセージ"
}

## 重要ルール
- 時刻なし create は 09:00
- end_at 未指定なら呼び出し側で +1h 補完
- 「夏季休暇」「出張」「研修」「旅行」など時刻なしで日単位の予定は all_day=true
- 「8/11-16 夏季休暇」「8/11から7日間 夏季休暇」「8/11〜8/16 出張」を期間予定として解釈
- 期間のend_atは「含む最終日」をGeminiに出させて、コード側で+1日して
  Google Calendar APIの date フィールド（end exclusive）に渡す
- タイトルに付いた括弧書き（「夏季休暇（棚卸し）」など、半角/全角/角括弧/【】）はタイトルに残す
- 「毎週月曜」「毎月15日」などはRRULE化（FREQ=WEEKLY;BYDAY=MO 等）

## 環境変数（.env）
- LINE_CHANNEL_ACCESS_TOKEN
- LINE_CHANNEL_SECRET
- LINE_USER_ID
- GEMINI_API_KEY
- GEMINI_MODEL=gemini-2.5-flash
- GOOGLE_CALENDAR_ID=primary
- TZ=Asia/Tokyo
- WEBHOOK_HOST=0.0.0.0
- WEBHOOK_PORT=5555

## LaunchAgent 3つ
- com.niki.schedule.webhook.plist: Flask常駐 (KeepAlive)
- com.niki.schedule.ngrok.plist: ngrok常駐 (KeepAlive、固定ドメイン使用)
- com.niki.schedule.notifier.plist: 毎朝8:00に notifier.py を実行

## エラー時の挙動
- どこで失敗しても LINEに「エラーが発生しました」と返す
- ログは logs/ に出す（webhook.log, notifier.log, ngrok.log）
- token.json が失効したらわかりやすいエラー表示

## テスト
- pytest で parser, calendar_client, notifier の主要パスをカバー
- Gemini と Google APIはモック

## ドキュメント
- README.md に起動手順、運用コマンド、トラブルシューティングを書く

仕様の不明点はその都度聞いてください。
````

### 5-3. Claude Codeとやり取り

途中で「これでいいですか」「動かしてみてください」と聞かれたら **Yes** で進めます。

完成後、Claude Codeが「`pip install -r requirements.txt` してください」みたいな指示を出すので、それに従います。

> 💡 **重要**: ngrokの固定ドメインや、各種APIキーが必要な箇所で「あなたのドメインは？」と聞かれたら、STEP 1〜4で取得したものを答えてください。

---

## STEP 6：設定ファイル `.env` を埋める

`~/Claude/スケジュール管理/.env` というファイルを開いて（テキストエディットでOK）、STEP 1〜3で取得した鍵を貼り付けます。

```
LINE_CHANNEL_ACCESS_TOKEN=（STEP 1-4でメモした長い文字列）
LINE_CHANNEL_SECRET=（STEP 1-4でメモした文字列）
LINE_USER_ID=（後で取る。空でOK）
GEMINI_API_KEY=（STEP 3でメモした文字列）
GEMINI_MODEL=gemini-2.5-flash
GOOGLE_CALENDAR_ID=primary
TZ=Asia/Tokyo
WEBHOOK_HOST=0.0.0.0
WEBHOOK_PORT=5555
```

> ⚠️ `.env` ・ `credentials.json` ・ `token.json` は**絶対に他人に渡さない**。GitHubに上げるときは必ず `.gitignore` で除外（Claude Codeに作ってもらった時点で入っているはず）。

### Googleカレンダー認証（初回1回だけ）

```bash
.venv/bin/python -m src.calendar_client
```

ブラウザが開いてGoogleログイン画面に → 「自分のアカウント」を選ぶ → 「許可」
これで `token.json` が自動生成されます。

---

## STEP 7：常時起動の設定（LaunchAgent）

LaunchAgent = Macが起動している間、常にプログラムを動かし続ける仕組み。

Claude Codeが生成した3つのplistファイルを所定の場所に配置します:

```bash
cp com.niki.schedule.webhook.plist ~/Library/LaunchAgents/
cp com.niki.schedule.ngrok.plist ~/Library/LaunchAgents/
cp com.niki.schedule.notifier.plist ~/Library/LaunchAgents/

launchctl load ~/Library/LaunchAgents/com.niki.schedule.webhook.plist
launchctl load ~/Library/LaunchAgents/com.niki.schedule.ngrok.plist
launchctl load ~/Library/LaunchAgents/com.niki.schedule.notifier.plist
```

### 動いているか確認

```bash
launchctl list | grep niki.schedule
curl -s http://localhost:5555/healthz       # ok と返ればFlaskが起動中
curl -s https://あなたのngrokドメイン/healthz  # ok と返れば外から繋がってる
```

---

## STEP 8：LINEに「外からの入口」を教える

STEP 4でもらったngrokのURLを、LINE側に登録します。

1. LINE Developers → さっき作ったMessaging APIチャネル → 「Messaging API」タブ
2. 「**Webhook URL**」に以下を入力（自分のドメインに置き換えて）:
   ```
   https://あなたのngrokドメイン/webhook/line
   ```
3. 「**Verify**」ボタンで疎通確認
4. 「**Use webhook**」を **オン**

---

## STEP 9：動作テスト

### 自分のUser IDを取る

LINEで自分のAI秘書に何か送ってみる → ターミナルでログを確認:

```bash
tail -f ~/Claude/スケジュール管理/logs/webhook.log
```

`source.userId = U1234567890abcdef...` のような行が出るので、これをメモ。

`.env` の `LINE_USER_ID=` に貼り付けて、再起動:

```bash
launchctl unload ~/Library/LaunchAgents/com.niki.schedule.webhook.plist
launchctl load ~/Library/LaunchAgents/com.niki.schedule.webhook.plist
```

### 試してみる

LINEで以下を送ってみる:

| 送るメッセージ | 期待される返信 |
|---|---|
| `今日15時にテスト` | ✅ 登録しました ... |
| `明日の予定教えて` | 一覧、または「予定なし」 |
| `8/11-16 夏季休暇` | ✅ 登録しました 8/11(火)〜8/16(日) 夏季休暇 終日 |
| `さっきのテストキャンセル` | 🗑 削除しました |

カレンダーアプリでも確認 → 予定が増えていれば成功！

### 朝の通知をテスト

明日まで待ちたくない場合:

```bash
cd ~/Claude/スケジュール管理
.venv/bin/python -m src.notifier
```

LINEに今日の予定が届けばOK。

---

## よくあるエラーと対処

### LINEから返信が来ない

```bash
tail -50 ~/Claude/スケジュール管理/logs/webhook.err.log
```

を見て、出ているエラーをそのままClaude Codeに貼り付けて聞いてください。

### `invalid_grant: Token has been expired or revoked`

Google認証が切れています。

```bash
cd ~/Claude/スケジュール管理
rm token.json
.venv/bin/python -m src.calendar_client
```

ブラウザで再度許可。

**再発するなら**: STEP 2-3 で「公開ステータス: 本番環境」になっているか確認。テストモードだと7日で切れます。

### ngrokのURLが変わってしまった

無料の**固定ドメイン**を使っていれば変わりません。もし変わっているなら、固定ドメインを設定していない可能性。STEP 4-3を再確認。

### Macをスリープしている間のメッセージが取りこぼされる

LINE仕様で、失敗したWebhookは再送されません。常用するなら:
```bash
sudo pmset -a sleep 0    # スリープを無効化（電源接続時）
```

---

## 改造のヒント

### 「お薬リマインダー」を毎晩追加したい
LaunchAgentをもう1つ作って、夜9時に固定メッセージをPushすればOK。Claude Codeに依頼してください。

### 家族用に複数人で使いたい
`LINE_USER_ID` をリストにして全員にPush。`config.py` の編集をClaude Codeに依頼。

### 「会議の議事録、Notionに保存して」もできる？
Notion API連携を追加すれば可能。STEP 5のプロンプトに「Notion APIで議事録ページを作成」を追加すればClaude Codeが作ってくれます。

### iPhoneのSiriから直接登録したい
LINEを経由せず、iOSショートカット → ngrok URL に直接POSTすればOK。Webhookエンドポイントを `/webhook/siri` のように追加するだけ。

---

## 最後に

このアプリは、外部サービス（LINE / Google / Gemini / ngrok）の組み合わせなので、**動かなくなったときは必ずどのサービスで詰まっているかを切り分ける**ことが大切です。

```bash
# 1. Flaskが動いているか
curl -s http://localhost:5555/healthz

# 2. ngrok経由で繋がっているか
curl -s https://あなたのドメイン/healthz

# 3. ログを見る
tail -50 ~/Claude/スケジュール管理/logs/webhook.err.log
```

の3つをまず確認すれば、原因はだいたい絞れます。あとはClaude Codeに「このエラーが出ました」と貼り付ければ、たいてい直してくれます。

楽しい自動化ライフを。
